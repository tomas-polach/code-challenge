const { getA, getB, getC } = require('./model/get-data.js');

async function getAplusB() {
  // TODO: get results from getA and getB, cache it and return the sum of these
}

async function getBplusC() {
  // TODO: get results from getB and getC, cache it and return the sum of these
}

module.exports = { getAplusB, getBplusC }